/**
 * Configuration Export Index
 * Central export point for all configuration modules
 * Usage: import { CredentialsManager, TestData, UIMessages, AppConstants } from '@config'
 */

export { CredentialsManager, type UserCredentials } from './credentials';
export { TestData } from './testData';
export { UIMessages } from './messages';
export { AppConstants } from './constants';
